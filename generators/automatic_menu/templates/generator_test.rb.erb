require File.dirname(__FILE__) + '/../../test_helper'
<% model_name = name.classify -%>

class <%= model_name %>GeneratorTest < ActionController::TestCase
  tests PagesController

  def test_<%= name %>_generator_should_run_generator
    locals, render_options = run_menu_generator(:<%= name %>, {
      # add generator options
	})
    assert_not_nil locals[:menu_items], "Error in <%= name %> menu generator. Need menu_items local"
  end

end
